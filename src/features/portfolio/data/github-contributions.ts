export const GITHUB_USERNAME = "sheikh162";

// This function is called by the component to get data. 
// For now, we are generating mock data inside the component itself (in graph.tsx),
// so this file can be minimal or just hold the username.
export function getGitHubContributions() {
  // In the future, you can add a fetch() call here to your backend API
  return [];
}